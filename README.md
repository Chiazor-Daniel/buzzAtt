# buzzAtt — Student Attendance App

A **React Native** attendance app built for **students** (companion to a lecturer app).

- **Student** — home
- **StudentCourse / Course** — enrolled courses
- **StudentProfile** — profile screen
- **Attendance** entry

## Run

```bash
npm install
# Android: npm run android   ·   iOS: npx pod-install && npm run ios
```